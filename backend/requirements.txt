fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.11
pydantic-settings>=2.10.1,<3.0.0
python-dotenv==1.0.1

# LangChain/LangGraph moved to a 1.0 line in Oct 2025 — pin the majors,
# let pip resolve the exact patch versions so they stay mutually compatible.
langgraph>=1.2,<2.0
langchain-core>=1.4,<2.0
langchain-groq>=1.1,<2.0
httpx>=0.28.1

# Dev / test / lint
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.9
